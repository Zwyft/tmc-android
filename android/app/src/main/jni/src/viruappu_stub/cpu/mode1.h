#pragma once

/* Stub for cpu/mode1.h — the real header from ViruaPPU.
 * Required by port_ppu.c's include of <cpu/mode1.h>.
 * Mode-1 specific definitions are merged into virtuappu.h. */
#include "virtuappu.h"
